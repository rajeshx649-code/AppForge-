# AppForge
A simple GitHub-style app publishing landing page and local demo.

## GitHub Pages
Upload `index.html`, `style.css`, and `script.js` to a GitHub repository and enable GitHub Pages.

Note: this first version saves app listings only in the visitor's browser. A real public publishing platform needs a backend/database and file storage.
